import { ButtonComponentConfig } from '@rapider/angular-components/button';
import { ButtonSize } from '@rapider/angular-components/core/button';

export interface ButtonGroupComponentConfig {
  buttons?: ButtonComponentConfig[];
  buttonSize?: ButtonSize;
}
