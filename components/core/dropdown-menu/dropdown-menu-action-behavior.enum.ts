export enum DropdownMenuActionBehavior {
  /* route when action is triggered */
  Router = 'router',
  /* only emit the data as output */
  Emit = 'emit',
  /* do nothing */
  NoAction = 'noAction'
}
